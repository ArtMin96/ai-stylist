// GENERATED by tools/codegen/gen-kernel.mjs from packages/shared-kernel/registry/entitlements.json. DO NOT EDIT: run `just generate`.

/// Value kind of an entitlement (planning/12 §3.1).
public enum EntitlementValueKind: String, CaseIterable, Sendable {
  case boolean = "boolean"
  case limit = "limit"
  case level = "level"
  case map = "map"
}

/// Entitlement name (planning/12 §3.1): the only entitlement identifiers.
public enum EntitlementName: String, CaseIterable, Sendable {
  /// Maximum closet items (null = unlimited)
  case closetMaxItems = "closet.max_items"
  /// Recommendations per local day
  case recsDailyLimit = "recs.daily_limit"
  /// Full context richness (hourly weather, holidays, all occasions)
  case recsContextFull = "recs.context.full"
  /// Future-day outfit planning
  case recsFuturePlanning = "recs.future_planning"
  /// Avatar ladder level (A1, A1 + all poses)
  case avatarLevel = "avatar.level"
  /// Outfit view generation ladder level (G0, G2)
  case tryonGenerative = "tryon.generative"
  /// Missing-view synthesis
  case viewsMissingView = "views.missing_view"
  /// Generative credits granted per month
  case creditsMonthly = "credits.monthly"
  /// Credit top-up packs purchasable
  case creditsTopup = "credits.topup"
  /// Versioned credit weights per generative task, carried on the plan
  case creditsWeights = "credits.weights"
  /// Trends feed level (basic, personalized)
  case trendsLevel = "trends.level"
  /// Wardrobe analytics
  case analyticsWardrobe = "analytics.wardrobe"
  /// Priority queue processing
  case processingPriority = "processing.priority"
  /// Multi-angle exports
  case exportMultiAngle = "export.multi_angle"
  /// Early-access features
  case featuresEarlyAccess = "features.early_access"
  /// Assistant chat (future)
  case chatStylist = "chat.stylist"
  /// Personal data export
  case dataExport = "data.export"

  /// Value kind of the entitlement.
  public var kind: EntitlementValueKind {
    switch self {
    case .closetMaxItems: .limit
    case .recsDailyLimit: .limit
    case .recsContextFull: .boolean
    case .recsFuturePlanning: .boolean
    case .avatarLevel: .level
    case .tryonGenerative: .level
    case .viewsMissingView: .boolean
    case .creditsMonthly: .limit
    case .creditsTopup: .boolean
    case .creditsWeights: .map
    case .trendsLevel: .level
    case .analyticsWardrobe: .boolean
    case .processingPriority: .boolean
    case .exportMultiAngle: .boolean
    case .featuresEarlyAccess: .boolean
    case .chatStylist: .boolean
    case .dataExport: .boolean
    }
  }

  /// Human-readable meaning (registry text, not UI copy).
  public var description: String {
    switch self {
    case .closetMaxItems: "Maximum closet items (null = unlimited)"
    case .recsDailyLimit: "Recommendations per local day"
    case .recsContextFull: "Full context richness (hourly weather, holidays, all occasions)"
    case .recsFuturePlanning: "Future-day outfit planning"
    case .avatarLevel: "Avatar ladder level (A1, A1 + all poses)"
    case .tryonGenerative: "Outfit view generation ladder level (G0, G2)"
    case .viewsMissingView: "Missing-view synthesis"
    case .creditsMonthly: "Generative credits granted per month"
    case .creditsTopup: "Credit top-up packs purchasable"
    case .creditsWeights: "Versioned credit weights per generative task, carried on the plan"
    case .trendsLevel: "Trends feed level (basic, personalized)"
    case .analyticsWardrobe: "Wardrobe analytics"
    case .processingPriority: "Priority queue processing"
    case .exportMultiAngle: "Multi-angle exports"
    case .featuresEarlyAccess: "Early-access features"
    case .chatStylist: "Assistant chat (future)"
    case .dataExport: "Personal data export"
    }
  }
}

/// Weighted generative task metered against `credits.monthly` (planning/12 §3.3).
public enum CreditMeter: String, CaseIterable, Sendable {
  case tryon = "tryon"
  case missingView = "missing_view"
}
