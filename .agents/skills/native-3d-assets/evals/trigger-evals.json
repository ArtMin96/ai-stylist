[
  {
    "query": "need to wire pose switching in apps/mobile/src/render so tapping a thumbnail in the avatar screen flips between the 4 P04 pose clips",
    "should_trigger": true
  },
  {
    "query": "the glTF export for anny-adult-v1 is missing KTX2 textures and just assets-validate is failing on it",
    "should_trigger": true
  },
  {
    "query": "garment attachment got renamed from leftSleeve to sleeve_L in the skeleton and now old avatar configs render sleeves in the wrong spot",
    "should_trigger": true
  },
  {
    "query": "want to add Draco compression to the base mesh export before it ships, current file size blows the doc 07 budget",
    "should_trigger": true
  },
  {
    "query": "react-native-filament crashes on a low-tier Android device when switching poses, need to profile frame time and memory",
    "should_trigger": true
  },
  {
    "query": "need a manifest schema update under assets/3d for the new G2 garment representation level",
    "should_trigger": true
  },
  {
    "query": "camera rotate/zoom gesture on the 3D avatar preview feels janky, suspect per-frame allocations in the render loop",
    "should_trigger": true
  },
  {
    "query": "morph target names in the new export don't match the shared-kernel registry, need to reconcile before merging",
    "should_trigger": true
  },
  {
    "query": "want to add a provenance + confidence overlay on the generated avatar view per the honesty invariant",
    "should_trigger": true
  },
  {
    "query": "the outfit recommendation keeps suggesting a coat + shorts combo that looks wrong once rendered on the avatar",
    "should_trigger": false,
    "_near_miss_reason": "surfaces through the 3D render, but 'which outfit gets suggested' is recommendation-rules' scoring problem, not a rendering/asset one."
  },
  {
    "query": "want to add a 'rotate the avatar' button on the outfit review screen, just the button and its onPress handler",
    "should_trigger": false,
    "_near_miss_reason": "mentions the avatar and rotation, but a bare UI button outside the render boundary is mobile-feature's screen-level work; only the actual rotate driver would be native-3d-assets."
  },
  {
    "query": "fal.ai virtual try-on generated image for a garment came back with the wrong lighting, need to fix the provider prompt",
    "should_trigger": false,
    "_near_miss_reason": "touches a generated garment view, but the fix is server-side AI image generation — media-ml-pipeline, not the client 3D pipeline."
  },
  {
    "query": "closet screen shows the wrong thumbnail for an item after upload, looks like a caching bug in the 2D photo grid",
    "should_trigger": false,
    "_near_miss_reason": "shares 'closet item image' vocabulary but is a 2D photo/mobile-screen bug — mobile-feature."
  },
  {
    "query": "want to measure API p95 latency for the avatar config endpoint after the last deploy",
    "should_trigger": false,
    "_near_miss_reason": "mentions avatar and measurement, but this is a server API latency question against a budget — performance-profiling (or backend-module for the fix), not the render/asset layer."
  },
  {
    "query": "add a `pose_id` column to the avatar_configs table and backfill existing rows with the default pose",
    "should_trigger": false,
    "_near_miss_reason": "shares 'pose' and 'avatar' vocabulary, but this is a schema + migration change on the backend table — db-migration."
  },
  {
    "query": "reviewing the PR that adds the new Draco mesh exporter script before it merges, want boundary/duplication eyes on it",
    "should_trigger": false,
    "_near_miss_reason": "the PR is native-3d-assets work, but reviewing it for boundary violations is architecture-review's task, not implementation."
  },
  {
    "query": "is it okay for the avatar module's backend service to read the user's measurement history directly from another module's table",
    "should_trigger": false,
    "_near_miss_reason": "mentions avatar, but this is a backend module-boundary question with no rendering or asset content at all — backend-module / architecture-review."
  },
  {
    "query": "write a haiku about autumn leaves for the app's seasonal marketing email",
    "should_trigger": false,
    "_near_miss_reason": "no shared vocabulary or plausible overlap with native-3d-assets at all; included only as a sanity floor, not a meaningful boundary test."
  }
]
