# dependencies / build
node_modules/
.turbo/
dist/
build/
coverage/
*.tsbuildinfo

# env & secrets (only *.enc.yaml under secrets/ is committed; `just secrets-sync` merges into .env)
.env
.env.*.local
secrets/*
!secrets/README.md
!secrets/*.enc.yaml
.direnv/
.mise.local.toml
mise.local.toml

# Expo / React Native (CNG: ios/ and android/ are generated by prebuild)
.expo/
.expo-shared/
expo-env.d.ts
ios/
android/
*.jks
*.keystore

# python
.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.pyc

# logs / OS
*.log
.DS_Store
Thumbs.db

# editors
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# security scan artifacts (`just security-scan` / `just sbom` write artifacts/sbom/, never committed)
artifacts/
sbom.*.json
gitleaks-report.json

# license-gate fixture: a hand-written node_modules tree that must be committed (docs/security/licenses.md)
!tools/security/fixtures/agpl-tree/node_modules/
.pnpm-store/
