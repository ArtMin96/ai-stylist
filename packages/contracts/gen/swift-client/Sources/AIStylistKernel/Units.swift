// GENERATED by tools/codegen/gen-kernel.mjs from packages/shared-kernel/registry/units.json. DO NOT EDIT: run `just generate`.

/// Canonical length unit(s) (planning/06 §2): the API stores and returns SI.
public enum LengthUnit: String, CaseIterable, Sendable {
  case cm = "cm"
}

/// Canonical mass unit(s) (planning/06 §2): the API stores and returns SI.
public enum MassUnit: String, CaseIterable, Sendable {
  case kg = "kg"
}

/// Canonical temperature unit(s) (planning/06 §2): the API stores and returns SI.
public enum TemperatureUnit: String, CaseIterable, Sendable {
  case celsius = "celsius"
}

/// Measurement dimension (TS `Dimension`; renamed to avoid Foundation.Dimension).
public enum MeasurementDimension: String, CaseIterable, Sendable {
  case length = "length"
  case mass = "mass"
  case temperature = "temperature"

  /// Raw value of the canonical (stored) unit for this dimension.
  public var canonicalUnit: String {
    switch self {
    case .length: "cm"
    case .mass: "kg"
    case .temperature: "celsius"
    }
  }
}

/// Canonical (stored) unit per dimension.
public enum CanonicalUnits {
  public static let length: LengthUnit = .cm
  public static let mass: MassUnit = .kg
  public static let temperature: TemperatureUnit = .celsius
}

/// Display unit system selectable in the profile (planning/06 §3.1 `locale.unitSystem`).
public enum UnitSystem: String, CaseIterable, Sendable {
  case metric = "metric"
  case imperial = "imperial"
}

/// Who produced a measurement value (planning/06 §3.1): estimated/derived values must carry confidence.
public enum MeasurementSource: String, CaseIterable, Sendable {
  case user = "user"
  case estimated = "estimated"
  case derived = "derived"
}

/// Exact display-conversion factors (planning/06 §3.1); storage stays SI.
public enum ConversionFactors {
  public static let cmPerInch: Double = 2.54
  public static let kgPerPound: Double = 0.45359237
}
