// GENERATED by tools/codegen/gen-kernel.mjs from packages/shared-kernel/registry/reason-codes.json. DO NOT EDIT: run `just generate`.

/// Reason-code namespace (planning/09 §7).
public enum ReasonCodeNamespace: String, CaseIterable, Sendable {
  /// hard exclusions (stage 3)
  case excl = "RC-EXCL"
  case weather = "RC-WEATHER"
  case occasion = "RC-OCCASION"
  case color = "RC-COLOR"
  case fit = "RC-FIT"
  case `repeat` = "RC-REPEAT"
  case rarelyWorn = "RC-RARELY-WORN"
  case pref = "RC-PREF"
  case trend = "RC-TREND"
  /// sparse closet
  case gap = "RC-GAP"
  case ctxMissing = "RC-CTX-MISSING"
  case stale = "RC-STALE"
}

/// Pipeline stage (planning/09 §2) that may emit a reason code.
public enum ReasonCodeStage: String, CaseIterable, Sendable {
  case exclusion = "exclusion"
  case scoring = "scoring"
  case diagnostic = "diagnostic"
}

/// Recommendation reason code (planning/09 §7): a stable identifier emitted while deciding; explanations are templated from it.
public enum ReasonCode: String, CaseIterable, Sendable {
  /// Excluded: feels-like temperature is below the safety threshold for this coverage
  case exclColdSafety = "RC-EXCL-COLD-SAFETY"

  /// Namespace the code belongs to.
  public var namespace: ReasonCodeNamespace {
    switch self {
    case .exclColdSafety: .excl
    }
  }

  /// Stage that may emit the code.
  public var stage: ReasonCodeStage {
    switch self {
    case .exclColdSafety: .exclusion
    }
  }

  /// Names of the typed template params (planning/09 §7).
  public var params: [String] {
    switch self {
    case .exclColdSafety: ["threshold", "feelsLike"]
    }
  }

  /// Human-readable meaning (registry text, not UI copy).
  public var description: String {
    switch self {
    case .exclColdSafety: "Excluded: feels-like temperature is below the safety threshold for this coverage"
    }
  }
}
