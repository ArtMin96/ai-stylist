[
  {
    "query": "need to build the closet grid screen in apps/mobile/src/features/closet, should show empty/loading/error states and call GET /v1/closet/items through the generated client",
    "should_trigger": true
  },
  {
    "query": "the onboarding flow's consent toggle isn't persisting when you background the app and come back — expo-router route under src/app/onboarding",
    "should_trigger": true
  },
  {
    "query": "want to add an offline upload queue for closet photos so captures survive a dropped connection",
    "should_trigger": true
  },
  {
    "query": "push notification tap should deep-link into the outfit detail screen, need to wire the expo-router route",
    "should_trigger": true
  },
  {
    "query": "RNTL test for the profile screen keeps flaking on the async render, can you fix the test and the screen if needed",
    "should_trigger": true
  },
  {
    "query": "add a camera capture UI for the closet item photo flow, needs to request permission and show a retry state on denial",
    "should_trigger": true
  },
  {
    "query": "the PostHog consent stub needs a settings row so users can opt out later, not just at onboarding",
    "should_trigger": true
  },
  {
    "query": "building a new Expo module wrapper around expo-haptics for a subtle vibration on swipe, nothing to do with 3D",
    "should_trigger": true
  },
  {
    "query": "outfit review screen needs a share button that exports the outfit summary as an image attachment",
    "should_trigger": true
  },
  {
    "query": "add a rotate/zoom gesture to the avatar preview so users can spin the 3D model on the outfit review screen",
    "should_trigger": false,
    "_near_miss_reason": "the screen is a mobile UI concern but the rotate/zoom control itself is Filament/render-boundary work — native-3d-assets, not mobile-feature."
  },
  {
    "query": "need a new GET /v1/closet/items/{id}/history endpoint returning a different shape than what the OpenAPI spec currently has",
    "should_trigger": false,
    "_near_miss_reason": "phrased as a closet screen need, but the actual change is the wire contract — api-contract-change goes first."
  },
  {
    "query": "trial users should lose access to the outfit-of-the-day screen the moment their RevenueCat entitlement lapses, but they're still seeing it",
    "should_trigger": false,
    "_near_miss_reason": "the symptom is on a mobile screen, but the defect is in entitlement gating logic — entitlements-billing owns that invariant."
  },
  {
    "query": "write the Maestro flow that taps through onboarding, opts into consent, and lands on the home screen",
    "should_trigger": false,
    "_near_miss_reason": "onboarding is mobile-feature's screen, but writing the e2e flow itself is e2e-device-testing's job now that apps/mobile/e2e/ has its own owner."
  },
  {
    "query": "the closet recommendation keeps suggesting outfits with items marked 'in laundry', need to exclude those from candidates",
    "should_trigger": false,
    "_near_miss_reason": "surfaces on a mobile screen, but excluding laundry items from candidate generation is a recommendation-rules constraint, not a screen change."
  },
  {
    "query": "fal.ai try-on job for a closet item never finishes, stuck in pending in pg-boss for over an hour",
    "should_trigger": false,
    "_near_miss_reason": "affects what the mobile app displays, but the actual failure is in the server-side job pipeline — media-ml-pipeline."
  },
  {
    "query": "reviewing the diff for the new offline-queue PR before merge, want a second pair of eyes on module boundaries",
    "should_trigger": false,
    "_near_miss_reason": "the diff is mobile-feature work, but reviewing it for boundary violations is architecture-review's task, not implementation."
  },
  {
    "query": "app startup feels sluggish on a mid-tier Android phone since the last release, need to figure out why",
    "should_trigger": false,
    "_near_miss_reason": "affects the mobile app, but startup-time investigation against a documented budget is performance-profiling's job, not a feature build."
  },
  {
    "query": "add a `low_stock_threshold` column to closet's item_states table and write the migration for it",
    "should_trigger": false,
    "_near_miss_reason": "shares 'closet' and a UI-adjacent feature name, but the ask is a schema + migration on the backend — db-migration, not a mobile screen change."
  },
  {
    "query": "write a haiku about autumn leaves for the app's seasonal marketing email",
    "should_trigger": false,
    "_near_miss_reason": "no shared vocabulary or plausible overlap with mobile-feature at all; a useless negative if it were the only one, included here only as a sanity floor."
  }
]
