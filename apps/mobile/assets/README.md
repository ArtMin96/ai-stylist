# App icons (placeholders)

`icon.png` and `adaptive-icon.png` are generated solid 1024×1024 placeholders so `expo prebuild`
succeeds. Real brand assets replace them before the first `preview` build (release-readiness skill).
