// Public API of the `fashion-intel` module (docs/modules/fashion-intel.md). Everything else lives in
// ./internal and is unimportable from outside (rule: public-api-only). P02: no providers yet.
import { Module } from '@nestjs/common';

@Module({})
export class FashionIntelModule {}
