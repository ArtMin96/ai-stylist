// Public API of the `billing` module (docs/modules/billing.md). Everything else lives in
// ./internal and is unimportable from outside (rule: public-api-only). P02: no providers yet.
import { Module } from '@nestjs/common';

@Module({})
export class BillingModule {}
