// Public API of the `closet` module (docs/modules/closet.md). Everything else lives in
// ./internal and is unimportable from outside (rule: public-api-only). P02: no providers yet.
import { Module } from '@nestjs/common';

@Module({})
export class ClosetModule {}
